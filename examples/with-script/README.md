# jankmeter — Script Example

Vanilla HTML/JS + jankmeter programmatic API. Uses Vite as a dev server only.

## Run

```sh
bun install
bun run dev
```

The jankmeter toolbar will appear at the bottom of the page.
