# jankmeter — Next.js Example

Next.js App Router + jankmeter via the `<JankMeter />` component from `jankmeter/next`.

## Run

```sh
bun install
bun run dev
```

The jankmeter toolbar will appear at the bottom of the page.
