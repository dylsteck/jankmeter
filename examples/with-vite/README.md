# jankmeter — Vite Example

Vite + React + jankmeter via Vite plugin.

## Run

```sh
bun install
bun run dev
```

The jankmeter toolbar will appear at the bottom of the page. Click the buttons to see metrics change in real time.
